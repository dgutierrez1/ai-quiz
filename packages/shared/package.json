{
  "name": "@ai-quiz/shared",
  "version": "0.0.0",
  "private": true,
  "description": "ai-quiz shared Zod schemas + scoring (pure functions over DTOs).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "echo 'dev: scaffold-only; scoring land in Story 1.2' && exit 0",
    "build": "echo 'build: scaffold-only; tsc in Story 1.2' && exit 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "~6.0.3",
    "vitest": "4.1.10"
  }
}