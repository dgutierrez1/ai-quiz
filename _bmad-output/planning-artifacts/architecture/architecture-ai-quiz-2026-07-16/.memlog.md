---
scope: greenfield NestJS+Next.js AI quiz generator; hexagonal; LLM-agnostic
purpose: produce architecture spine to bound implementation across API/web/DB/LLM
altitude: initiative
updated: 2026-07-19T20:26
---

- (decision) AD-1 paradigm=Hexagonal Architecture. Binds: apps/api/src/* (entire backend). Prevents: domain logic coupled to NestJS/Drizzle/Mastra; cross-boundary types duplicated. Rule: domain/ ports/ use-cases are pure; adapters and driving layer hold all I/O. -- [ADOPTED from SPEC.md §4]
- (decision) AD-2 domain_purity: apps/api/src/domain/ imports nothing from @nestjs/*, drizzle-orm, mastra, undici, node:fetch. Binds: apps/api/src/domain/* + all use-cases. Prevents: I/O leaking into pure logic; tests becoming slow/integration-y. Rule: enforced by ESLint no-restricted-imports rule plus domain tsconfig with separate package boundary.
- (decision) AD-3 zod_dto_boundary: All cross-boundary types are Zod-inferred DTOs (z.infer of X), Object.freeze-wrapped at construction. Binds: HTTP to domain, domain to adapter, adapter to domain. Prevents: any/unknown leaking across layers; runtime shape drift. Rule: never pass plain objects across boundaries; always parse + freeze.
- (decision) AD-4 llm_untrusted: LLM outputs are untrusted. Binds: every adapter that calls an LLM. Prevents: malformed JSON, refusal strings, injection-shaped strings reaching persistence/UI. Rule: Zod safeParse + 2 retries (best-effort providers) / 1 retry (strict-mode); UntrustedLlmOutputError on final failure; session marked failed.
- (decision) AD-5 llmport_only: Only LlmPort (apps/api/src/domain/ports/LlmPort.ts) calls LLM adapters. Binds: all LLM-touching code. Prevents: bypass of capability map, caching, retry, output validation. Rule: use-cases never import from adapters/llm/* directly.
- (decision) AD-6 provider_agnostic_mastra: Mastra resolves LLM providers via env vars and 'provider/model' string format. Binds: LLM adapter, runtime config. Prevents: hardcoded provider SDK in domain; per-provider auth leaks. Rule: LLM_MODEL default 'groq/openai/gpt-oss-20b'; MiniMax wired via inline url form (not built-in Mastra provider).
- (decision) AD-7 mastra_catchall_last: MastraModule.register({mastra}) MUST be imported last in AppModule imports. Binds: apps/api/src/app.module.ts. Prevents: catch-all @All('*') shadowing all NestJS controllers (returns 404 for every endpoint). Rule: integration test asserts every NestJS route resolves; comment in app.module.ts above MastraModule explaining the constraint.
- (decision) AD-8 runtime_constraints: Node >= 22.13.0 (pin in package.json engines); @nestjs/platform-express only (Mastra adapter does not support Fastify). Binds: apps/api runtime, deploy target. Prevents: Mastra integration failure; silent Fastify incompatibility. Rule: CI fails on Node <22.13.0; lock @nestjs/platform-express as direct dep.
- (decision) AD-9 rls_per_request: All /sessions/:id/* routes verify ownership via @OwnsSession() interceptor + Postgres RLS. Binds: all session-scoped endpoints. Prevents: IDOR (S2); cross-user data leak. Rule: interceptor opens transaction with SET LOCAL app.user_id = '<validated-uuid>'; RLS policies on ALL user-owned tables (quiz_sessions, chat_messages, insights, documents, questions, answers, user_responses, knowledge_categories); returns 404 (not 403) for both not-found and not-owned.
- (decision) AD-10 ssrf_expanded: HttpMarkdownAdapter implements SSRF defense with expanded IP blocklist. Binds: IngestionPort. Prevents: SSRF to RFC1918 + CGN (100.64/10) + Oracle IMDS (192.0.0/24) + benchmarking (198.18/15) + multicast + reserved + IPv6 ULA + 6to4 (2002::/16); IPv4-mapped IPv6 bypass; IDN homograph; HTTP/0.9 smuggling. Rule: scheme allowlist https/http only; hostname blocklist (localhost, metadata.*); net.isIP() for literals; normalize ::ffff:a.b.c.d then a.b.c.d before RFC1918 check; DNS-pin-then-validate; bind undici Agent to validated IP; reject punycode mismatch; disable redirects; 10MB cap; 10s timeout; text/markdown or text/plain only.
- (decision) AD-11 identity_hmac: X-User-Id spoofing mitigated via HMAC binding + IP-fallback rate limit. Binds: every authenticated endpoint. Prevents: per-user rate limit bypass via UUID rotation (S5). Rule: client sends X-User-Hmac = HMAC(USER_HMAC_SECRET, userId||timestamp); server verifies and rejects if older than 5min; IP-fallback 5 req/sec/IP regardless of UUID.
- (decision) AD-12 chat_pre_submit_guard: Chat endpoint while session.status='ready' strips question text + correct answers from LLM context. Binds: POST /api/sessions/:id/chat. Prevents: answer-exfiltration before submit (S1). Rule: use-case checks status; ChatUseCase receives redacted QuestionDto if status='ready'; full DTO if status='submitted'.
- (decision) AD-13 tavily_dual_llm: Tavily web search results are summarized by a secondary LLM call to <=200 chars before being added to the main agent's context. Binds: WebSearchPort implementation; chat flow with tools. Prevents: tool-result poisoning (indirect prompt injection). Rule: summarizer cannot invoke tools; raw HTML/markdown never crosses the boundary; summarizer uses different provider than main agent when possible.
- (decision) AD-14 drizzle_only_persistence: Drizzle ORM is the only persistence path; raw SQL via sql tag is allowed only inside adapters and must use parameterized queries. Binds: all DB-touching code. Prevents: SQL injection; data-access duplication. Rule: no other ORMs/raw pg client; migrations via drizzle-kit; sql template tag uses $1/$2 placeholders.
- (decision) AD-15 submit_idempotency: user_responses has UNIQUE(session_id, question_id); submit transitions quiz_sessions atomically via UPDATE WHERE status='ready' RETURNING. Binds: POST /api/sessions/:id/submit. Prevents: double-scoring on retry/duplicate POST; lost responses. Rule: DB constraint plus transaction wrapper; concurrent submits return the cached result (idempotent).
- (decision) AD-16 multi_answer_scoring: Multi-answer scoring = round(4 * |correct intersect selected| / |correct|, 2), capped at 4. Binds: scoring module in packages/shared. Prevents: multi-answer questions never scoring 4; NaN from empty input. Rule: full-correct yields 4; empty yields 0; throws on n<=0; type='single' requires exactly 1 correct; type='multiple' requires 2..4 correct; positions validated as unique integers in [0,3].
- (decision) AD-17 fe_state_split: Server state owned by TanStack Query v5; client state via React Context (UUID/theme) + useState per-component + custom useLocalStorage hook for prefs. Binds: apps/web state management. Prevents: state store bloat; SSR hydration mismatches; cross-component prop drilling. Rule: no Zustand, no Redux; query-keys factory in apps/web/lib/queries.ts; UUID generated + persisted in top-level script before React hydrates.
- (decision) AD-18 lazy_sdk_loading: Provider SDKs are dynamic-imported in LlmAdapter on first use. Binds: apps/api/src/adapters/llm/*. Prevents: 256MB Fly free-tier OOM; 150MB baseline from loading 5 SDKs. Rule: default provider (groq) loaded eagerly; others via await import() in MastraLlmAdapter per call; /api/health exposes process.memoryUsage() in non-prod.
- (decision) AD-19 deploy_topology: Vercel (web) + Fly.io (API) + Neon (DB). Binds: deployment + cost ceiling. Prevents: violating $0/mo mandate. Rule: env vars via fly secrets set + Vercel per-env; migrations via fly.toml release_command; node:20-slim base image; auto-stop Fly machines with cron ping /healthz every 4min (min_machines_running unavailable on free tier).
- (decision) AD-20 health_endpoints: Two health endpoints split by purpose. Binds: Fly healthcheck + monitoring. Prevents: Fly kill-loop when Neon is cold (P5). Rule: GET /healthz is process-alive (~1ms), used by Fly healthcheck (timeout 30s, grace 60s, interval 30s); GET /api/health deep-checks DB + provider reachability, used by monitoring; Drizzle init wrapped in 5-attempt retry loop with exp backoff.
- (version) stack: NestJS 10 + @mastra/nestjs 6.10.0 + Drizzle 0.30+ + Postgres 16 (Neon free tier); Next.js 15 App Router + Tailwind + shadcn/ui + framer-motion + TanStack Query v5; pnpm workspaces monorepo; Vitest 1.x; Playwright 1.4x. All per PRD Addendum A.
- (constraint) constraint: providers MUST be enabled only when their env keys are set. GET /api/config/providers filters out unset providers. Default is groq/openai/gpt-oss-20b when GROQ_API_KEY is present.
- (constraint) constraint: tier-1 providers (default + first fallbacks) include groq + openai + anthropic + MiniMax (intl + cn). Tier-2 fallbacks (opt-in only): ollama (local). Any future provider addition requires BMM architecture update.
- (constraint) constraint: web UI must NOT include LLM provider SDKs in its bundle. All LLM calls happen server-side via the NestJS API. Confirmed via bundle analyzer + CI step.
- (event) spine finalized: 20 ADs + 3 constraints + 1 version + 1 event (this one) logged; ARCHITECTURE-SPINE.md distilled from memlog. status=final.
- (decision by user) AUDIT 2026-07-16 — adversarial review of SPEC archive; 10 resolutions accepted by user. The PRD (prd-ai-quiz-2026-07-16/prd.md) was updated and is authoritative. SPEC archive is frozen/unmaintained/NOT authoritative — never re-derive a rule from it. Spine below is PRE-AUDIT and must be regenerated.
- (decision by user) SUPERSEDES AD-9 rls_per_request: RLS is REMOVED. Ownership = @OwnsSession() interceptor + app-layer WHERE user_id = ? + 404 for not-found AND not-owned. No SET LOCAL app.user_id, no RLS policies, no transaction interceptor. Reason: policies could not work as written — documents/questions/answers/user_responses/knowledge_categories have no user_id column (join via session_id); and without FORCE ROW LEVEL SECURITY the Neon table-owner role bypasses RLS entirely. Control was illusory. Interceptor + app filter is now the SOLE defense and must be airtight. Isolation test stays, reframed from RLS to ownership.
- (decision by user) SUPERSEDES AD-11 identity_hmac: HMAC binding REMOVED. No X-User-Hmac header, no USER_HMAC_SECRET, no rotation. Reason: the browser had to compute it, so the secret shipped client-side and forging was free — zero security value plus quarterly rotation that breaks live clients. Replacement: every route rate-limited twice, once keyed by X-User-Id and once by IP, same table, stricter wins (global 30/min, POST /sessions 5/min, /insight 10/min, /chat 20/min). The old 5 req/sec/IP fallback is ALSO removed — it was looser than the 30/min per-user limit it backed (300/min vs 30/min), giving a UUID-rotating attacker a 10x budget. In-memory throttler store accepted but REQUIRES max_machines_running = 1.
- (decision by user) SUPERSEDES AD-19 deploy_topology (base image only): node:22-slim, NOT node:20-slim. Reason: Mastra requires Node >= 22.13.0, so node:20 fails the engines check and breaks the API in prod. Rest of AD-19 (Vercel + Fly + Neon, fly secrets, release_command migrations, auto-stop + cron ping /healthz) stands, PLUS max_machines_running = 1 is now REQUIRED for rate-limiter correctness.
- (decision by user) SUPERSEDES scoring AD (was: round(4 x |correct AND selected| / |correct|, 2) capped at 4): multi-answer is now clamp(round(4 x (hits - misses) / |correct|, 2), 0, 4) where hits=|correct AND selected|, misses=|selected NOT IN correct|. Wrong picks cancel right picks. Fully-correct=4, partial=proportional, hit+miss=0, SELECT-ALL=0, empty=0. Reason: the old formula ignored wrong selections, so selecting all 4 options scored FULL MARKS on every multiple question — a scoring-integrity defect in the core deliverable, not an accepted trade-off. Chosen over Jaccard, which only halves select-all to 2.0. Also NEW: submissions must be complete (one response per question, IDs matching session set, else 400) which makes weightedFinalScore's contiguous-position invariant hold by construction.
- (decision by user) NEW (no prior AD): FR-15 ingest neutralization + output grounding. Ingested markdown is inert data; only execution surfaces are the generator LLM (content integrity) and the chat tool loop (bounded, accepted). Ingest: deterministic LLM-free scrub — strip HTML comments/raw HTML/link titles/alt text/base64 blobs, NFKC normalize, strip control+zero-width+bidi chars, PRESERVE code blocks. NEVER LLM-rewrite the source doc (rewriter is itself injectable, costs a critical-path pass, destroys quiz fidelity). Output: structured output is the containment; Q/A render as PLAIN TEXT never markdown; DOMPurify scoped to explanations+chat only; grounding check rejects questions with no token overlap vs source chunks; secret-shaped-token check relative to source. REMOVED: outputLooksUnsafe keyword blocklist (DROP TABLE/password/<script>) — false-positived on the DB/security READMEs this app targets while protecting nothing the above doesn't; and the heuristic prompt-injection regex (theatre).
- (decision by user) NEW (no prior AD): FR-16 bounded critical path. SYNC path to status=ready: fetch -> neutralize -> chunk -> select ~8k-token chunk budget -> 1 structured LLM call -> persist -> return. Chunking STAYS sync (prerequisite for generation, costs ms). ASYNC via void enrich(sessionId) after response: full doc+chunk persistence, the >12k-token MAP-REDUCE, chat cache prefix, knowledge_categories aggregates, Langfuse flush. No queue/Redis/BullMQ. INVARIANT: enrichment is an optimization NEVER a correctness dependency — if Fly auto-stop kills it, consumers recompute on demand. Map-reduce must never sit on the generation critical path. ALSO: generation is closed-world — NO Tavily/web search during generation (only the single ssrf-safe-fetch of sourceUrl); Tavily confined to insight + chat. The grounding check depends on this closed-world property.
- (decision by user) SUPERSEDES insight flow: BOTH insight kinds (explanation, gap_analysis) require status='submitted' -> 409 otherwise. The 'OR strip question text from context' escape hatch is REMOVED — it left /insight as the unguarded twin of the guarded chat endpoint, reopening the answer-exfiltration vector. Also CORS: exact WEB_ORIGIN always; WEB_ORIGIN_REGEX only when NODE_ENV !== 'production'; the x-vercel-environment gate is removed (that header never reaches Fly from a browser). Also provider label fix: gpt-oss-20b is GROQ-hosted (groq/openai/gpt-oss-20b), strict json_schema, 1 retry — the archive mislabeled it 'OpenAI gpt-oss-20b'.
- (event by user) Spine ARCHITECTURE-SPINE.md is PRE-AUDIT and stale (AD-9 RLS, AD-11 HMAC, AD-19 node:20, old scoring formula at L181, HMAC in L213/472/527, RLS in L261/473/541). User chose to REGENERATE it via /bmad-architecture in a fresh chat rather than hand-patch. Upstream contamination vectors were cleaned first: project-context.md (constitution) rules 2/4/5/6/7/8 corrected + scoring section added; PRD frontmatter demoted the SPEC archive from 'companions' to 'historical_only'; these memlog corrections appended. Regenerate from PRD + project-context.md ONLY.
- (event) SPINE REGEN STARTED 2026-07-18. Update intent (existing run detected, stale-pre-audit spine from before 2026-07-16 audit). Capturing audit + extraction deltas as new memlog entries before re-distilling. Inputs: PRD (464 lines, post-extraction, product-only), architecture-spec.md (399 lines, post-extraction, implementation detail), project-context.md (constitution), this .memlog.md. Override: _bmad/custom/bmad-architecture.toml adds 2 project-specific reviewers (AD-vs-implementation cross-check; FR-1..17 → Capability Map). -- [RESUME — preserving AD-1 ID]
- (event) SPINE DISTILLED 2026-07-18 (Update intent — replaced stale-pre-audit spine). 27 ADs total: 8 new (AD-N1..N8) + 4 amended (AD-6/9/16/19) + 1 removed (AD-11 HMAC → absorbed into AD-N7) + 14 unchanged. 4 ADs that were stale-by-construction now corrected: AD-9 RLS → ownership-only, AD-11 HMAC → removed, AD-16 wrong formula → clamp with miss-cancellation, AD-19 node:20 → node:22. ERD updated (insights.kind='gap_analysis' only; INDEX+UNIQUE added). 5 valid mermaid blocks. Frontmatter status=draft (becomes 'final' after reviewer gate passes).
- (event) SPINE FINALIZED 2026-07-18. Reviewer Gate complete: 4 reviewers ran in parallel (web-research verification, AD adversarial compatibility, implementation cross-check, FR coverage). Findings: 2 critical composition gaps fixed (AD-N6 ownership-then-status ordering, AD-N5 position-from-question_id mapping), 4 AD amendments (R2.1/2.2/2.3/2.4 — grounding retry semantics, package boundary enforcement, concurrent-submit recovery, AD-N5 position binding), ERD error_message column added, 2 NFRs now have ADs (Observability=AD-N9, Testing=AD-N10), Capability Map now references AD-N2/AD-N4 for FR-3, concrete paths for §10.1 Security. Status=final. Total: 29 ADs (10 new + 4 amended + 1 removed AD-11 + 14 unchanged). Line count ~770. Skipped: tech version pin web-verification (R1.1-R1.5) — requires web access; flagged for human review at next deploy. Next BMAD workflow: bmad-create-epics-and-stories (now unblocked).
- (event) READINESS CHECK 2026-07-18: bmad-check-implementation-readiness completed. Verdict: NEEDS WORK. PRD (464 lines, 16 active FRs + 8 NFRs), spine (status=final, 29 ADs), architecture-spec (399 lines), project-context (164 lines) all present and consistent. Single blocker: epics & stories document does not exist — Phase 4 cannot start. UX design intentionally out of scope (per PRD §5/§6.2). Tech version pins flagged for web verification before scaffold. Report at _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-18.md. Next BMAD step (in fresh chat): /bmad-create-epics-and-stories.
- (change) REVIEW-FOLLOWUPS.md created 2026-07-18 to track 13 unresolved review findings (10 of 13 already applied as spine fixes during regen). Items categorized by doc owner: 3 spec items (R3 F1, F3, F4), 3 spine trivial edits (R1 F11, F12, R4 F5), 7 spine items requiring human/web verification (R1 F1-F5, F6-F8, F10). Process: edit lowest-authority doc that owns the change; log in that doc's memlog; if higher doc affected, re-run that workflow (Update intent). bmad-correct-course is NOT the right tool here — it requires epics (Phase 4) which we don't have yet.
- (change) REVIEW-FOLLOWUPS 2026-07-18: applied 4 trivial edits from REVIEW-FOLLOWUPS.md. R1 F11 — 'Select-all → 0' wording tightened to 'Select-all (or any over-selection) → 0 (math: hits − misses = 0)' at spine L191. R4 F5 — FR-13 path in Capability Map corrected from 'domain/services/CategoryAggregator' to 'domain/quiz/services/CategoryAggregatorService' at spine L694. R3 F1 — architecture-spec.md L245 MiniMax-M3 row label clarified ('MiniMax-M3 (Groq not; MiniMax is its own provider — see Default below)' instead of 'MiniMax-M3 (default)'). R3 F3 — architecture-spec.md L133 insights.kind enum corrected to 'gap_analysis' only; L162 POST /api/sessions/:id/insight row corrected to 'gap_analysis only (whole quiz).' 9 R1 items (web-research version pins + citations) remain flagged for human pre-deploy verification — no spine edit until then.
- (change) REVIEW-FOLLOWUPS 2026-07-18 second pass: (1) OQ-1 RESOLVED — default provider changed from groq/openai/gpt-oss-20b to minimax/MiniMax-M3 (user-specified; flagship; 1M context; auto caching only). Applied to spine AD-6 + capability matrix + OQ-1 row, PRD §10 + assumption A-6, architecture-spec.md §A.6 + §A.9 + row 175 JSON example, project-context.md. Groq remains opt-in via provider dropdown. (2) R1 F12 — FR-8 row in Capability Map now includes test file path 'apps/api/test/security/ownership.test.ts — cross-user returns 404'. (3) REVIEW-FOLLOWUPS.md summary updated: 7/13 items applied, 6 remaining (all R1: F1-F5 version pins, F6-F8 model/citation flags, F10 deployment). Process refined: version pins are resolved at first pnpm install (defer from pre-deploy to install); citations are real-world signals at first use, not pre-deploy verifications.
- (change) Default provider propagation: 2 stale 'groq' references found and fixed after the MiniMax default change. (1) project-context.md L161 — 'Default LLM' now reads minimax/MiniMax-M3 (was groq/openai/gpt-oss-20b). (2) spine L212 AD-18 — 'Default provider (minimax/MiniMax-M3) loaded eagerly' (was groq). Remaining 'groq' references in spine are correct (provider enum list, providers/ subfolder comment, LLM providers mermaid node — all are provider lists, not default claims). (3) PRD §8 OQ-1 text now strikethrough + 'RESOLVED 2026-07-18' marker. (4) architecture-spec.md L174 JSON example now uses provider: 'minimax'. All 4 docs (PRD + spec + spine + project-context) now consistent: default = minimax/MiniMax-M3; Groq remains an opt-in provider via the dropdown.
- (event) VALIDATION 2026-07-19: spine version pins + model IDs reality-checked via pnpm view + provider docs (https://platform.minimax.io/docs/api-reference/text-anthropic-api, https://docs.anthropic.com/en/docs/about-claude/models, https://console.groq.com/docs/models). Corrections applied across spine + spec + PRD + project-context: (1) Tailwind 3.x → 4.3.3 (v4 is a major rewrite — Oxide engine, CSS-first); (2) @nestjs/core 10.x → 11.1.28; (3) Zod 3.x → 4.4.3 (HIGH — affects every DTO); (4) Vitest 1.x → 4.1.10 (jumped 3 majors); (5) framer-motion 11.x → motion 12.42.2 (renamed Nov 2024; React imports use motion/react); (6) @mastra/nestjs 6.10.x → 0.2.7 (was off by 30x on the subpackage version); (7) @mastra/core → ^1.50.0 (1.51.0); (8) capability matrix now 'verified 2026-07-19' with per-row provider doc URLs; (9) model IDs: claude-sonnet-4-6 → claude-sonnet-5 (current default; 4-6 is now Legacy); Sonnet 4-6 context window 200k → 1M (matches Sonnet 5 / Opus 4.6+); MiniMax-M3 unchanged (correctly named). Only R1 F10 (deployment) remains for actual deploy-time validation. REVIEW-FOLLOWUPS.md updated: 9 R1 items now Applied, 1 Open (deployment). Spine is now fully validated for planning purposes; ready for /bmad-create-epics-and-stories.
- (event) PRD UPDATE 2026-07-19 PROPAGATED to spine: (1) AD-6 Rule rewritten — v1 supports MiniMax only (no OpenRouter free, no other pay-as-you-go). (2) Capability matrix simplified — MiniMax-M3 row only; other providers' rows removed. (3) FR-13 row in Capability Map marked REMOVED — gap analysis via chat. (4) source tree comment 'providers/' updated to 'minimax only (v1 scope)'. (5) RequestInsightUseCase.ts comment updated to REMOVED. (6) OQ-1 row in Deferred updated — already resolved as MiniMax default.
- (event) PRD UPDATE 2026-07-19 round 2 PROPAGATED to spine: (1) AD-6 Rule reverts to MiniMax + OpenRouter free models (was: MiniMax only). Capability matrix adds OpenRouter free row filtered by pricing.prompt='0'. (2) AD-15 Rule extended: submit returns {finalScore, breakdown[], categoryBreakdown[], insights:{topicsToStudy[], weakCategories[], strengthByCategory}} — single response, no POST /insight endpoint. (3) AD-N3 Rule extended: content-density check — doc_tokens / questionCount < ~500 tokens/question returns 400 DOC_TOO_SHORT with hint to reduce questionCount. (4) FR-7 row in Capability Map updated to 'inline results + insights'. (5) FR-10 row updated to note chat context includes submit-time insights. architecture-spec.md capability matrix row added for OpenRouter free; spine AD-6 provider Rule + AD-15 + AD-N3 all updated.
- (change) OWNERSHIP DECISION 2026-07-19 propagated: AD-9 Rule updated with three-layer enforcement + companion lint rule @ai-quiz/no-unscoped-session-query + v2 RLS deferred pattern (function-based + FORCE + EXISTS-join). Project-context §Security rule 2 updated with companion v1 lint + v2 deferred. AGENTS.md 'do not weaken' section updated with 3 new items (no user_id columns on child tables; no weakening interceptor; RLS-v2 must use function-based with FORCE).
- (change) PR propagation of per-session cost removal: (1) Consistency Conventions row 'Per-session cost budget' marked REMOVED 2026-07-19. (2) AD-N9 cost tracking paragraph replaced with removal note. (3) ERD cost_spent column mark updated to 'REMOVED 2026-07-19 — column dropped from v1 schema'. (4) Deferred row 'Per-user cost limits (quota UI)' marked REMOVED. (5) Spec was already clean (no /bin/zsh.50/cost_spent references — the 2026-07-16 audit had removed budget references from spec; PRD and spine re-introduced them in error during regen). Net: 4 changes, all in spine only. PRD and spec remain aligned with spine on the no-budget decision.
- (event) READINESS CHECK 2026-07-19: bmad-check-implementation-readiness re-run after 2026-07-19 updates. Verdict: NEEDS WORK. Single blocker: epics & stories document does not exist. All 16 active FRs + 8 NFRs mapped in PRD; all 29 ADs in spine consistent with PRD + project-context + architecture-spec. Recent changes verified consistent: UJ rewrite, FR-12/13 removed (chat absorbs gap analysis), mobile first-class, MiniMax+OpenRouter free, no per-session budget, content-density check (DOC_TOO_SHORT), v1 ownership decision. 20/21 REVIEW-FOLLOWUPS items resolved. Report at _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-19.md. Status will move NEEDS WORK → READY after /bmad-create-epics-and-stories.
- (change) RLS UPGRADE 2026-07-19: AD-9 Rule expanded to 4-layer enforcement (interceptor + use-case + session-scope + RLS). RLS upgraded from v2-deferred to v1 default per user direction. Migration pattern: function current_session_user_id() + ALTER ENABLE + FORCE + function-based policies. The 'v2 hardening (deferred — not in v1)' block is removed; replaced with the v1 four-layer pattern + the 'both lint and RLS are required' rationale. The 'Companion hardening rule' section now correctly describes it as dev-time enforcement that catches before RLS, with RLS as runtime defense-in-depth. Pending v1 Drizzle migration: a single SQL file that creates the function + 7x ALTERs + 7x CREATE POLICY.
- (change) TOOLING CONVENTIONS ADDED 2026-07-19 (user direction): spine Consistency Conventions table now includes 4 new rows for tooling (v1): (1) Linting — ESLint flat config with 3 mandatory custom rules: @ai-quiz/no-unscoped-session-query, @ai-quiz/require-data-testid, @ai-quiz/no-console-log. (2) Formatting — Prettier 3.x with .prettierrc, runs on pre-commit + pre-push + CI. (3) TypeScript — strict + noUncheckedIndexedAccess + noImplicitOverride; tsconfig.base.json shared, per-package override. (4) Scripts — pnpm dev/build/test/test:e2e/format/lint/db:migrate/typecheck; pnpm verify = CI gate = lint:check && typecheck && test && test:e2e && build. project-context.md updated with parallel 'Tooling Rules' section referencing the spine. AGENTS.md already has the 'Tooling' cross-reference. Net: 4 docs consistent on tooling config (project-context = constitution, spine = conventions table, AGENTS.md = pointer, spec = pre-implementation reference — to be retired).


Additional fixes 2026-07-19: (11) Added mistralai/mistral-small-3.1-24b-instruct:free to AD-6 OpenRouter example list (was 3 examples, now 4 matching PRD §10.7).


[2026-07-19] CANONICAL ADAPTER PATTERN applied across 5 docs: spine AD-3 has the canonical code pattern (Object.freeze(Schema.parse(row)) inside every adapter method, canonical adapter-method pattern). spec §A.3 has the full TypeScript example (QuizSessionSchema + DrizzleSessionRepository + SubmitAnswersUseCase). spec §A.1 rule 4 updated to canonical wording. PRD §10.9 added (canonical adapter pattern cross-references). project-context §Architecture Rules rule updated. AGENTS.md Hexagonal section updated. All 5 docs now consistent on the canonical DTOs-at-boundaries pattern.
- (correction by user) Residual to fix on next spine re-distill (found 2026-07-19 during create-epics-and-stories): AD-N7 rate-limit table still lists 'POST /api/sessions/:id/insight 10/min' although AD-N6 + FR-12/FR-13 removal (2026-07-19) deleted the insight endpoint. Drop that row on regen. PRD §10.2 + constitution rule 7 already corrected (insight row removed). No hand-edit to the rendered spine per memlog-derived rule.
