SPINE REFINEMENTS 2026-07-19 (from bmad-architecture regen + 4 parallel reviewer subagents):

(1) **AD-N6 removed** — FR-12/FR-13 insight endpoints gone; gap analysis delivered via chat (FR-10) + inline in /submit (AD-15). 7 spine references to FR-13/RequestInsightUseCase.ts cleaned up.

(2) **AD-9 RLS** — added AsyncLocalStorage requirement for `SET LOCAL app.user_id` propagation (separate Drizzle connections would silently return 0 rows). Function `current_session_user_id()` is `LANGUAGE sql STABLE` and does NOT use `missing_ok=true` (broken composition fails loud).

(3) **Stack + ERD fixes:**
  - Provider enum: `minimax|openrouter` (was `groq|openai|anthropic|ollama|minimax`)
  - providers/ source-tree comment updated
  - OpenRouter examples: 4 model IDs including mistralai/mistral-small-3.1-24b-instruct:free (was 3 examples)

(4) **cost_spent row deleted from ERD** (was just annotated; should be gone).

(5) **AD-N10 test results now include gap analysis + insights** per PRD §10.4 — implementation: Vitest reporter hook + Playwright `testInfo` annotation pulling gap data from `quiz_sessions` (post-submit fixtures).

(6) **AD-N7 UUID regex tightened to canonical v4** (`^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`) — the loose 8-4-4-4-12 was accepting v1/v2/v3/v5/v6/v7 too and defeated the random-UUID assumption.

(7) **FR count claims**: 16 → 15 (FR-12 + FR-13 both removed). Updated in 3 places (frontmatter, Handoffs, Final Notes).

(8) **AD count**: 27 → 29 (AD-6 + AD-11 removed, AD-N1-N10 added, AD-N6 later removed; net +2).

(9) **OQ-2 mobile row updated to RESOLVED** — mobile is a first-class v1 surface.

(10) **AGENTS.md Security section** updated: 3-layer/RLS-removed → 4-layer/RLS-v1 to match spine.

Total: 10+ fixes across spine + AGENTS.md. Minor v1 review-r1 web-research items (Fly.io/MiniMax tier specifics) flagged for human verification. Status: ready for `/bmad-create-epics-and-stories`.
