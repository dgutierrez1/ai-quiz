---
topic: ai-quiz UX — DESIGN.md + EXPERIENCE.md spines
updated: 2026-07-19T23:34
---

- (decision) Working mode: Fast path — batch gaps, draft both spines with [ASSUMPTION] tags, skip creative tools.
- (decision) Sources inherited by reference: prds/prd-ai-quiz-2026-07-16/prd.md (canonical), architecture/architecture-ai-quiz-2026-07-16/ARCHITECTURE-SPINE.md + specs/architecture-spec.md, epics.md. Subagent-extracted at Discovery.
- (event) Source extraction complete (PRD, architecture spine + spec, epics). IA scaffold recovered: 3 routes (/, /quiz/[id], /result/[id]) + ~25 named components, each traceable to a story.
- (event) GAP: epics.md states verbatim 'None — no UX design contract exists for this project.' These spines are the missing contract; stories 2.5, 3.2, 3.3, 4.3, 5.1 depend on them.
- (event) GAP: visual identity NOT STATED across all sources — no palette, typography, spacing scale, icon set, or theme values. DESIGN.md has no upstream input to inherit; must come from user.
- (event) GAP: accessibility NOT STATED anywhere — no WCAG, ARIA, keyboard, focus, contrast, touch-target, or reduced-motion requirement in PRD, architecture, or epics. Only data-testid (testability, not a11y).
- (event) GAP: loading/empty/error states unspecified for /quiz/[id], /result/[id], and chat panel. Only 'no providers configured' + 'retry button not infinite spinner' are defined, the latter by negation.
- (decision) NO POLLING in v1. User explicitly never agreed to a 202+poll flow. Generation stays sync HTTP with a blocking wait. The architecture's reserved status='pending' escape hatch is NOT adopted by the UX; EXPERIENCE.md specs no polling affordance. If the ~30s ceiling is breached, the remedy is cutting the retry budget, not adding polling.
- (decision) Chat exists ONLY on /result/[id]. No chat surface on /quiz/[id] or landing. Consequence: the AD-12 pre-submit redacted-chat state is unreachable via UI (a 'ready' session routes to /quiz/[id], which has no chat). Server-side redaction guard remains as defense-in-depth; EXPERIENCE.md specs no pre-submit chat state because none exists.
- (override) CORRECTION to facilitator's earlier framing: generation is ONE LLM call on the happy path, as designed. The 'up to 3 calls' figure is the worst-case retry budget (<=2 strict-mode / <=3 best-effort), fired only when the pool fails validation and is wholly regenerated. Do not present multi-call as the normal flow.
- (assumption) Blocking waits are accepted as the nature of v1 (user: 'yes some requests are blocking, that is the nature'). Applies to both long waits: generation at POST /sessions and insight computation at POST /submit. UX must make waiting legible, not eliminate it.
- (decision) AESTHETIC DIRECTION: 'Calm study surface' — Notion/Readwise energy. Generous whitespace, comfortable reading measure, soft neutrals, warm accent. Low-pressure, unintimidating; leans into comprehension over assessment. A wrong answer must read as information, not judgment. All DESIGN.md tokens derive from this direction.
- (decision) GENERATION WAIT: narrated stages. Client-side timed narration of the real pipeline ('Fetching document... Reading... Writing questions...'). Purely presentational — NO server round trip, NO polling. Makes the 5-30s wait feel bounded and honest. Same treatment applies to the submit-time insight wait.
- (decision) ACCESSIBILITY FLOOR: WCAG 2.2 AA. Full keyboard operability, visible focus, 4.5:1 text contrast, labelled controls, prefers-reduced-motion honored, 24px minimum touch targets. Binding on both spines; testable.
- (decision) actualCount IS surfaced to the user, with a remedy nudge. Copy must frame it as a property of the document, never as user error or system failure — no scolding tone.
- (event) DESIGN.md and EXPERIENCE.md drafted (status: draft). All DESIGN.md contrast pairings computed and verified against WCAG 2.2 AA before commit; accent and strength-mixed were darkened from initial candidates (3.49:1 and 3.02:1) to #A85A2B and #8A6420 to clear 4.5:1.
- (override) DROPPED architecture spec §A.11 'hover pulse' on strength chips. A pulsing weak-category chip draws anxious attention to precisely the element the 'wrong answer is information, not judgment' posture is designed to make safe to look at. Score count-up, card transition, page fade/slide, and chat slide-in are retained.
- (decision) Breakpoints set by UX (none existed upstream): sm <640, md >=768, lg >=1024. The lg/1024px boundary is the only structural one — dual-panel collapses to tabs and sidebar becomes a sheet there. /quiz/[id] is single-column at every width.
- (assumption) Unresolved and carried as [ASSUMPTION] in EXPERIENCE.md: quiz/result loading skeletons; chat sending + error states; chat empty-state seed copy and quick-action chips; Enter/Shift+Enter chat send; 'thinking' reasoning_details not rendered in v1.
- (decision) Reviewer Gate declined and key-screen mocks declined by user. Spines are spine-only — no mockups/ or wireframes/ directory. Validate mode remains available later in a fresh chat.
- (override) Editorial doc_standards passes (bmad-editorial-review-structure, bmad-editorial-review-prose) NOT run. User chose the cheap path twice at Finalize; running two further skill passes would contradict that intent. Spines are final without an editorial pass — flagged rather than silently skipped.
- (event) spines finalized
