{
  "name": "@ai-quiz/api",
  "version": "0.0.0",
  "private": true,
  "description": "ai-quiz NestJS API — hexagonal architecture (domain / adapters / driving).",
  "type": "module",
  "scripts": {
    "dev": "echo 'dev: scaffold-only; NestJS wires up in Story 1.3' && exit 0",
    "build": "echo 'build: scaffold-only; NestJS wires up in Story 1.3' && exit 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'test: scaffold-only; integration tests land in Story 1.4' && exit 0"
  },
  "dependencies": {
    "@ai-quiz/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "~6.0.3",
    "vitest": "4.1.10"
  }
}
