# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/
*.tsbuildinfo

# Env files
.env
.env.local
.env.*.local
.env.development
.env.production
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Coverage / test artifacts
coverage/
.nyc_output/
playwright-report/
test-results/
playwright/.cache/

# Fly.io
.fly/

# Drizzle generated migrations (keep in repo) — only ignore local dumps
*.dump

# BMAD installer (re-installable via `npx bmad-method install`) — keep local only
_bmad/
.claude/skills/
.agents/skills/
.opencode/commands/

# BMAD installer-managed user overrides (never committed)
_bmad/custom/config.user.toml

# BMAD memlog scratch (keep architecture-ai-quiz-* in repo)
_bmad-output/**/*.memlog.md.tmp

# Local scratch
output/
*.local